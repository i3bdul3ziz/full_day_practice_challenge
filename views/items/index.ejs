<h1>item Page</h1>


<div>
    <% item.forEach( item => { %>
        <div>
            <p>item Name: <%= item.name %></p>
            <form action="items<%=item"></form>
        </div>
    <% }) %>
</div>